name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: self-hosted
    steps:
      - run: echo "The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "test echo 2"
      - run: pwd    
      - run: |
          echo ${{ github }}
          echo "GITHUB_REF = $GITHUB_REF"
          echo "all done..."
          #      - run: sh ./discordWebHookTest.sh    
