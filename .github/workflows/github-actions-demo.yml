name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: self-hosted
    steps:
      - run: echo "The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "test echo 2"
      - run: pwd    
      - run: sh ./discordWebHookTest.sh    
      - run: |
          cat ${{ github }} > /tmp/github.object.dat
          echo "all done..."
